---
title: "Spending"
format: html
---

```{r}
#| include: false
library(dashboardr)
```

[← Back to World Map](../index.html#)

# Ecuador

Top 20 advertisers by spending.

```{r, echo=FALSE, results='asis'}
dashboardr::enable_inputs()
```

```{r setup}
#| echo: false
#| warning: false
#| message: false
#| error: false
#| results: 'hide'

# Load required libraries
library(dashboardr)
library(dplyr)
library(highcharter)

# Global chunk options
knitr::opts_chunk$set(
  echo = FALSE,
  warning = FALSE,
  message = FALSE,
  error = FALSE,
  fig.width = 12,
  fig.height = 8,
  dpi = 300
)

# Load data
data <- readRDS('dataset_small_20obs.rds')

# Data summary
cat('Dataset loaded:', nrow(data), 'rows,', ncol(data), 'columns\n')

# Create filtered datasets
# Each filter is applied once and reused across visualizations

data_filtered_7e630601 <- data %>% dplyr::filter(page_group == "Politicians")
data_filtered_f010f38b <- data %>% dplyr::filter(page_group == "Other")
data_filtered_f290d8a9 <- data %>% dplyr::filter(page_group == "Government")

```


```{r}
#| echo: false
#| results: 'asis'
dashboardr::render_input_row(list(
  list(
    input_id = "advertiser_filter",
    label = "Filter Advertiser:",
    type = "select_multiple",
    filter_var = "advertiser",
    options = NULL,
    options_from = "advertiser",
    default_selected = NULL,
    placeholder = "Select...",
    width = "300px",
    min = 0,
    max = 100,
    step = 1,
    value = NULL,
    show_value = TRUE,
    inline = TRUE,
    toggle_series = NULL,
    override = FALSE,
    labels = NULL,
    size = "md",
    help = NULL,
    disabled = FALSE,
    mt = NULL,
    mr = NULL,
    mb = NULL,
    ml = NULL
  )
), style = "boxed", align = "center")
```

## Advertiser Type


::: {.panel-tabset}

### Total


```{r advertiser-type-total}
# All Advertisers
result <- create_bar(
  data = data,
  title = "All Advertisers",
  horizontal = TRUE,
  x_var = "advertiser",
  weight_var = "total_spend"
)

result
```


### Politicians


```{r advertiser-type-politicians}
# Politicians
result <- create_bar(
  data = data_filtered_7e630601,
  title = "Politicians",
  horizontal = TRUE,
  x_var = "advertiser",
  weight_var = "total_spend"
)

result
```


### Other


```{r advertiser-type-other}
# Other
result <- create_bar(
  data = data_filtered_f010f38b,
  title = "Other",
  horizontal = TRUE,
  x_var = "advertiser",
  weight_var = "total_spend"
)

result
```


### Government


```{r advertiser-type-government}
# Government
result <- create_bar(
  data = data_filtered_f290d8a9,
  title = "Government",
  horizontal = TRUE,
  x_var = "advertiser",
  weight_var = "total_spend"
)

result
```


:::

